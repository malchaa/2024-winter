#include<bits/stdc++.h>
using namespace std;

int main(void){
    ios::sync_with_stdio(0);
    cin.tie(0);
    int index;
    string s;
    cin >> s >> index;
    cout<< s[index - 1];
    return 0;
}
