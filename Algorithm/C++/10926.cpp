#include<bits/stdc++.h>
using namespace std;
int main(void){
    ios::sync_with_stdio(0);
    cin.tie(0);
    string input;
    cin >> input;
    cout << input + "?\?!" << "\n";
    // 삼중자 ??! << 가운데에 \ 삽입
    // cout << input << "??!";
    return 0;
}